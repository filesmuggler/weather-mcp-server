version: 1.0
runtime: python311

build:
  commands: "pip3 install --upgrade pip && pip3 install -r requirements.txt"

run:
  command: "uvicorn main:app --host 0.0.0.0 --port 8000"
  network:
    port: 8000
