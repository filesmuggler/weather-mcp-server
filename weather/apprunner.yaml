version: 1.0
runtime: python311
build:
  commands:
    build:
      - echo "Installing dependencies..."
      - pip3 install --target=/app/packages -r requirements.txt
run:
  command: PYTHONPATH=/app/packages:$PYTHONPATH python3 main.py
  network:
    port: 8000
