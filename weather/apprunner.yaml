version: 1.0
runtime: python3.11

build:
  commands:
    build:
      - echo "Installing dependencies..."
      - pip install --target=/app/weather/packages -r requirements.txt

run:
  command: python3 /app/weather/main.py
  env:
    - name: PYTHONPATH
      value: "/app/weather/packages:$PYTHONPATH"
  network:
    port: 8000
